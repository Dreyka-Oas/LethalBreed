# Gradle
.gradle/
build/
# Loom / dev runtime
run/
# IDE
.idea/
*.iml
.vscode/
.settings/
.classpath
.project
# OS
*.log
# Claude Code local skills/config — not pushed
.skills/
.claude/
# opencode local agent config — not pushed
opencode.json
# Dev run-output dumps (gradle log captures)
rc.txt
rs.txt
# Cloudflare Wrangler local cache
.wrangler/
# OS junk
.DS_Store
Thumbs.db
# Nested LethalBreed-web repo (separate, private git repo — do not track/embed)
/web/
